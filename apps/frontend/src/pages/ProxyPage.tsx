

export default function ProxyPage() {
    return (
        <div className="p-8">
            <h1 className="text-3xl font-bold mb-4">API Proxy</h1>
            <p className="text-gray-400">Configure local proxy server and providers.</p>
        </div>
    );
}
